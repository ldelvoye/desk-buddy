# Optional highest-precedence override for SQLite URL.
# If unset, runtime falls back to `config.toml`, then `%LOCALAPPDATA%/DeskBuddy/desk-buddy.db`.
# Examples:
# - sqlite://C:/Users/you/AppData/Local/DeskBuddy/desk-buddy.db
# - sqlite::memory:
DESK_BUDDY_DATABASE_URL=
